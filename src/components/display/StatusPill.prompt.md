Inline status dropdown — the primary way status changes, from any row.

```jsx
<StatusPill status="busy">Bezig</StatusPill>
<StatusPill status="todo" size="md">Te doen</StatusPill>
```

The ⌄ is part of the component; do not add your own caret.

A project in the sidebar or in the optional project column. Long project names truncate — they are never allowed to wrap.

```jsx
<ProjectItem name="HGD-traject 24-089" done={3} total={7} project={1} />
<ProjectItem name="Verontrusting 24-102" done={1} total={5} project={2} variant="progress" />
```

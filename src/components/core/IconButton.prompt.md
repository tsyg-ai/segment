Small square utility button for panel close / overflow actions. Icon only, so `label` is required.

```jsx
<IconButton icon="x" label="Paneel sluiten" />
<IconButton icon="ellipsis" label="Meer acties" />
```
